#include<stack>
#include<iostream>

using namespace std;

int main()
{
    int t;
    while(t--){
        stack<char> s;
        string st;
        getline(st, cin);
        s.push(st[0]);
        for(int i = 1;i<st.size();i++){
            if(st[i])
        }
    }
}
